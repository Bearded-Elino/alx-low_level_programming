#include <stdio.h>

/**
 * main - A program that utilizes the printf function to write a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
