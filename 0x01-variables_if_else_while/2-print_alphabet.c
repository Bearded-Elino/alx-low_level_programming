#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char v;
		v = 'a';
		while (v <= 'z')
		{
			putchar (v);
				c++;
		}
	putchar ('\n');
	return (0);
}
